package qualified_this {
    ~qtA~define A(val a:Int) {

      inner ~qtB~define B() {
        val x = `qtB`this`qtB`@B
        val y = `qtA`this`qtA`@A
        val z = `qtB`this
        ~xx~val Int.xx get() = `xx`this : Int
        ~xx()~fun Int.xx() {
            `xx()`this : Int
            val a = (fun Int.() = `xx()`this`xx()`@xx + this)

        }
      }
    }
}


define A<~T~T, ~E~E> {
  val a : `T`T
  val x : A<`T`T, `E`E>

  inner define X<~X.T~T> : A<`X.T`T, `E`E> {
    val a : `X.T`T
    val b : `X.E`E

    ~X.E~define E {}
  }
}

